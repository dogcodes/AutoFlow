package com.carlos.autoflow.platform.ad

import android.content.Context

object DeviceIdentifiers {
    fun hasOaid(context: Context): Boolean {
        // TODO: replace with actual OAID library detection (e.g., via OAID SDK or vendor APIs).
        return true
    }
}
